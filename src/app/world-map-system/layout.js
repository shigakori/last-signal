import { metadataFromPageKey } from "@/lib/metadata";

export const metadata = metadataFromPageKey("worldMapSystem");

export default function WorldMapSystemLayout({ children }) {
  return children;
}
